#########################################
# Generate processes

# Import model with massive photons
import model sm_ma

# Generate Process
generate u u~ > a a g , (g > d d~)

# Define output folder
output standalone_cpp standalone_uubar_aag_ddbar

# Look at the diagrams
display diagrams ./standalone_uubar_aag_ddbar
